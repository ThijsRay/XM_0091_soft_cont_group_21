apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "socon.fullname" . }}-test-connection"
  labels:
    {{- include "socon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "socon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
